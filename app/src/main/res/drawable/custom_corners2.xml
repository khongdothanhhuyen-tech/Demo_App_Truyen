<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Đặt màu nền -->
    <solid android:color="@color/gray_text_transparent" />
    <!-- Đặt viền bo góc -->
    <stroke
        android:width="2dp"
        android:color="@color/gray_text_transparent"/>
    <!-- Đặt bán kính bo góc -->
    <corners android:radius="10dp" />
</shape>